<h1>投稿一覧ページ</h1>

<div>ジャンル一覧検索</div>

<div>新着投稿</div>
	<% @posts.each do |post| %>
		<%= link_to post.title, post_path(post.id)  %>
		<%= post.body %>
		(いいね数：<%= post.favorited_users.count %>)
		(コメント数：<%= post.comments.count %>)
	<% end %>

<%= link_to "投稿画面へ", new_post_path %>