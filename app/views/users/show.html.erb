<h1>投稿者ページ</h1>

<div>ユーザー名</div>
	<%= @user.name %>
	<%= link_to "プロフィールを編集", edit_user_path(@user.id) %>
<div>投稿一覧</div>
	<% @posts.each do |post| %>
		<%= link_to post.title, post_path(post.id) %>
		<%= post.body %>
	<% end %>
